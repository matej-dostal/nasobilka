import type { ModeEnum } from "../interfaces";

export const Modes = {
    MULTIPLICATION: { label: "Násobilka", value: "multiplication" },
    POWER: { label: "Mocniny", value: "power" },
} as ModeEnum;
